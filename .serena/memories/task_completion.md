After code changes: regenerate code if prefs/models/routes require it (for typed_prefs run build_runner), format changed Dart files, analyze at least the changed files, and run targeted tests when the task affects tested behavior. For UI/settings changes, verify desktop-specific code paths when relevant (window manager, tray, startup). Do not edit generated *.g.dart files manually unless they were produced by generation in the same task.