Windows-oriented commands for this repo: `flutter pub get`, `dart run build_runner build --delete-conflicting-outputs`, `flutter analyze`, `flutter test`, `flutter run -d windows`, `flutter run -d linux`, `flutter run -d macos`, `rg pattern lib`, `rg --files lib`. Prefer MCP servers for Dart/Flutter analysis/formatting where available; use terminal when generation/build steps are needed.