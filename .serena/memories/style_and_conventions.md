Follow AGENT.md and docs-ai first. Prefer readability, maintainability, explicitness, and one source of truth. Use existing shared UI components and existing feature structure. Use async/await instead of then/catchError. Use Riverpod 3 providers/notifiers without @riverpod codegen. Use Toaster for user-facing errors and core logger helpers instead of print(). Freezed is for immutable models/state, not business logic/services. For preferences, edit *_prefs.dart and regenerate the typed_prefs *.g.dart files via build_runner.