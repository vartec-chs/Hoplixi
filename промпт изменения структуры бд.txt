Ты рефакторишь архитектуру базы данных менеджера паролей.

Технологии:
Flutter
Drift (SQLite)
SQLCipher
UUID primary keys
DAO pattern
Strong typing (Dart classes + Drift tables)

❗ Цель

Переделать текущую схему БД так, чтобы:
✅ можно было добавлять новые типы сущностей (api_key, secret, ssh_key и т.п.)
✅ была полноценная SQL-фильтрация по ВСЕМ полям
✅ не использовались JSON / EAV
✅ не было одной гигантской таблицы с nullable колонками
✅ была нормализованная история изменений
✅ архитектура была масштабируемой
✅ всё работало быстро в SQLite
✅ схема была типизированной и чистой

🚫 ЗАПРЕЩЕНО

Хранить payload в JSON
Использовать EAV
Использовать blob snapshot history
Делать одну таблицу со всеми возможными полями
Использовать polymorphic nullable колонки

✅ ОБЯЗАТЕЛЬНЫЙ ПАТТЕРН

Использовать Table-Per-Type (вертикальное разбиение).

🧱 Базовая таблица vault_items

Содержит ТОЛЬКО общие поля (проанализируй схему бд)

⚠️ НИКАКИХ специфичных полей тут быть не должно.

🧩 Таблицы типов

Каждый тип имеет свою таблицу пример:

password_items

itemId (PK + FK → vault_items.id ON DELETE CASCADE)

login

email

password (BLOB encrypted)

url

notes

С индексами:

login

email

url

otp_items

itemId

issuer

account

secret (BLOB)

period

digits

algorithm

bank_card_items

itemId

holder

last4

network

expiry

encrypted_pan

api_key_items (пример нового типа)

itemId

service

keyName

apiKey (BLOB)

📎 Tags

Единая таблица:

item_tags:

itemId

tagId
PRIMARY KEY(itemId, tagId)

🕒 HISTORY (КРИТИЧНО)

История должна быть тоже Table-Per-Type.

vault_item_history

id (UUID)

itemId

type

name

categoryId

action (created / updated / deleted)

createdAt

password_history

historyId (PK + FK)

login

email

url

password

notes

Аналогично для otp_history, bank_card_history и т.д.

⚠️ История должна позволять SQL фильтрацию по полям.
Никаких blob snapshot.

⚡ Производительность

JOIN только по PRIMARY KEY
Индексы на searchable поля
Предполагается ≤ 10k записей
Read-heavy workload
Никаких SELECT *

📦 Нужно сгенерировать:
Drift tables
Drift DAOs
Индексы
Foreign keys

Примеры запросов:
поиск пароля по login
поиск OTP по issuer
фильтрация history по login
Dart entity models
Enum типов сущностей
Repository abstraction
Архитектура должна быть production-grade.
НЕ УПРОЩАТЬ.

🧠 Контекст

Это password manager.
Безопасность важнее простоты.
Типы будут расширяться.
UI активно использует фильтрацию.

P.s

Обратная совместимость не нужна
Переделать все имеющиеся сущности
Все поля которые у сущностей есть сейчас должны остаться