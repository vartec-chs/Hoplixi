# План развития

## Баги на момент версии 1.0.0

1. ~~импорт из keepas - после выхода с экрана запускается проверка удалённой
   версии стора~~
2. ~~импорт otp не перекидывает на нужный route~~

## Доработки

1. инкрементация количества использований не должна влиять на инкрементацию
   версии стора
2. ~~при заходе на экран открытия хранилища со списком хранилищ не должен
   запускаться функционал связанный с cloud sync только тогда когда пользователь
   нажмёт на "открыть" в секции~~
3. ~~проверить корректность таймингов запроса сохранённого пароля в модалке
   открытия хранилища он должен запрашивать только при нажатии кнопки открыть
   для проверки или лучше его вообще не запрашивать пусть пользователь
   самостоятельно введёт пароль а бд ответить тот это пароль или нет~~
4. ~~для секций категорий добавить задержку перед показом loader и добавить
   анимацию появления элементов категорий и тегов~~

## Нововведения

1. ~~профили для генератора паролей~~
2. добавить массовое удаление элементов в dashboard
3. ~~добавить чтение банковских карт с помощью nfc_manager~~
4. Password Health Dashboard Добавь анализ безопасности: слабые пароли дубликаты
   старые пароли утечки (HaveIBeenPwned API)
5. Panic Mode ввод “специального PIN” → открывается “пустой vault”
6. ~~Ограничить количество неверных попыток ввода пароля для сторов~~
7. Command Palette Даже если есть поиск — это другой уровень UX. Как сделать:
   глобальный хоткей (Ctrl + K) ввод: git → GitHub аккаунт otp → сразу OTP
   действия: Enter → копировать пароль Shift+Enter → открыть запись
