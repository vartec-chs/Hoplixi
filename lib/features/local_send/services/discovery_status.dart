/// Статус сервиса обнаружения устройств.
enum DiscoveryStatus {
  /// Сервис остановлен (до первого запуска или после [dispose]).
  stopped,

  /// Сервис запускается (создаются сокеты, поднимается mDNS).
  starting,

  /// Сервис работает — реклама и сканирование активны.
  running,
}
